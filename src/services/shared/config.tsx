export const canvasPxSize = window.innerHeight / 1.8;
export const animationPatternTimeWidthWhenDurationIsZero = 10; // Used to prevent the pattern from rendering 0px width, so you can still click it
